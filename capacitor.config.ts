import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kotharibrothers.medicalledger',
  appName: 'Medical Ledger',
  webDir: 'dist'
};

export default config;
